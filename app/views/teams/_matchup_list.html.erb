<div id="matchup-list">
  <ul class="matchups">
    <% if @matchups.any? %>
      <h3>Team Matchups</h3>
      <%= render partial: 'matchups/simple_matchup', collection: @matchups, as: :matchup %>
    <% end %>
  </ul>
</div>